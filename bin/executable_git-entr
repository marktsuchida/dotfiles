#!/bin/bash
echo "Reminder: Use 'watchexec' instead of 'git entr -[c]dds'" >/dev/stderr
echo "    watchexec [-c] [--] command [args]" >/dev/stderr
echo "    ('-c': clear screen before running)" >/dev/stderr
exit 1

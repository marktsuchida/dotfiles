#!/bin/bash
# For Claude Code 'Notification' hook
input="$(cat)"
title="$(echo "$input" | jq -r '.cwd' | sed "s|^$HOME|~|")"
msg="$(echo "$input" | jq -r '.message')"
$HOME/bin/notify "$title" "$msg"

{{- /* vim: set filetype=gitconfig: */ -}}
[color]
	ui = auto
[credential]
{{- if (or (eq .chezmoi.os "darwin")
           (eq .chezmoi.os "windows")) }}
	helper =
	helper = manager
{{- else }}
	helper = cache --timeout=7200
{{- end }}
[merge]
	conflictstyle = diff3
	tool = vimdiff
	guitool = gvimdiff
[push]
	default = simple
[pull]
	ff = only
[diff]
	dirstat = files,0
	tool = vimdiff
	guitool = gvimdiff
[alias]
	graph = log --graph
[hub]
	protocol = https
[init]
	defaultBranch = main
[user]
	useConfigOnly = true
[rebase]
	autosquash = true
